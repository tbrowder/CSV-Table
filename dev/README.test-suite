The test generator will create individual test files
in separate directories named for the type of line-ending;

  nl
  dpipe

Each test file will be named by its comment-char (mark)
and separator char (sepchar):

  csv-{$CC}-{$SC}.t
  csv-{$CC}-{$SC}.csv
